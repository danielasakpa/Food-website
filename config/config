const dotenv = require("dotenv");
dotenv.config();

const config = {
 port: process.env.PORT || 5000,
 jwtSecret: process.env.JWT_SECRET || "ogfundz304",
 mongoUri: process.env.CONNECT_URL
}
module.exports = config;